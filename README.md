# AnySwap - Advanced Liquidity Management Platform

## 🚀 О проекте

AnySwap - это продвинутая платформа для управления ликвидностью на Uniswap V3, которая позволяет пользователям:
- Создавать и управлять позициями в пулах ликвидности
- Отслеживать доходность в реальном времени
- Использовать продвинутую аналитику для оптимизации позиций
- Управлять позициями через веб-интерфейс без прямого взаимодействия с Uniswap

## 🏗 Архитектура

### Технический стек:
- **Frontend**: Next.js 14, TypeScript, TailwindCSS
- **Web3**: Wagmi, Viem, RainbowKit, Uniswap V3 SDK
- **Backend**: Node.js, PostgreSQL, Redis
- **Аутентификация**: Email + Web3 кошельки
- **Интернационализация**: i18next (EN, RU, CN)

### Ключевые особенности:

#### 1. Двойная система авторизации:
- **Email регистрация**: 
  - 6-значный код подтверждения
  - Безопасное хранение паролей (bcrypt)
  - JWT токены для сессий
- **Web3 кошельки**:
  - MetaMask, Coinbase, WalletConnect
  - Binance Wallet, Uniswap Wallet
  - Подпись сообщений для аутентификации

#### 2. Управление позициями:
- **Smart Account Architecture** для управления позициями от имени пользователей
- Депонирование средств на смарт-контракт платформы
- Автоматическое реинвестирование комиссий
- Оптимизация ценовых диапазонов

#### 3. Аналитика в реальном времени:
- Отслеживание TVL, объемов, APR
- История позиций и доходности
- Импermanent loss калькулятор
- Прогнозирование доходности

## 📋 Установка и запуск

### Требования:
- Node.js 18+
- PostgreSQL 14+
- Redis 6+

### Шаги установки:

1. **Клонирование репозитория:**
```bash
git clone https://github.com/your-org/anyswap-platform.git
cd anyswap-platform
```

2. **Установка зависимостей:**
```bash
npm install
```

3. **Настройка переменных окружения:**
Создайте файл `.env.local` в корне проекта:
```env
# Database
DATABASE_URL=postgresql://user:password@localhost:5432/anyswap
REDIS_URL=redis://localhost:6379

# Email Configuration
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@domain.com
SMTP_PASS=your-app-password

# Web3
NEXT_PUBLIC_ALCHEMY_ID=your-alchemy-id
NEXT_PUBLIC_WALLETCONNECT_ID=your-walletconnect-id

# JWT
JWT_SECRET=your-super-secret-jwt-key

# API Keys
UNISWAP_API_KEY=your-uniswap-api-key
```

### 📝 Откуда взять значения переменных окружения:

#### **База данных (Database):**

1. **DATABASE_URL**:
   - **Да, используйте именно такую строку в `.env.local`**, но замените значения на свои:
     ```
     DATABASE_URL=postgresql://username:password@localhost:5432/database_name
     ```
   
   - **Как узнать свои значения:**
     - **username** (обычно `postgres`): имя пользователя PostgreSQL, которое вы указали при установке
     - **password**: пароль, который вы установили для PostgreSQL
     - **localhost:5432**: стандартный адрес и порт (обычно не меняется)
     - **database_name**: имя базы данных (например, `anyswap`). Если базы еще нет, создайте её:
       ```bash
       # Подключитесь к PostgreSQL
       psql -U postgres
       
       # Создайте базу данных
       CREATE DATABASE anyswap;
       
       # Выйдите
       \q
       ```
   
   - **Пример для локальной установки:**
     ```
     DATABASE_URL=postgresql://postgres:mypassword@localhost:5432/anyswap
     ```
     ⚠️ **Замените `postgres`, `mypassword` и `anyswap` на свои значения!**
   
   - Для облачных сервисов (Supabase, Neon, Railway): скопируйте Connection String из панели управления

2. **REDIS_URL**:
   - **Да, используйте именно такую строку:**
     ```
     REDIS_URL=redis://localhost:6379
     ```
   
   - **Установка Redis (если еще не установлен):**
     - macOS: `brew install redis` → `brew services start redis`
     - Linux: `sudo apt install redis-server` → `sudo systemctl start redis`
     - Windows: скачайте с [redis.io](https://redis.io/download)
   
   - **Проверка работы Redis:**
     ```bash
     redis-cli ping
     # Должен вернуть: PONG
     ```
   
   - Для облачных сервисов (Upstash, Redis Cloud): скопируйте URL из панели управления

#### **Email конфигурация (SMTP):**

3. **SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS**:
   - **✅ Да, можно использовать личный Gmail для локальных тестов!**
   
   - **Gmail настройка:**
     ```
     SMTP_HOST=smtp.gmail.com
     SMTP_PORT=587
     SMTP_USER=ваш-email@gmail.com
     SMTP_PASS=ваш-пароль-приложения
     ```
   
   - **Как создать "Пароль приложения" для Gmail:**
     1. Перейдите в [myaccount.google.com](https://myaccount.google.com)
     2. Безопасность → **Двухэтапная аутентификация** (должна быть включена)
     3. Прокрутите вниз до "Пароли приложений" → **Создать**
     4. Выберите "Почта" и "Другое устройство" (или "ПК")
     5. Введите название (например, "AnySwap Dev")
     6. Скопируйте **16-значный пароль** (без пробелов)
     7. Используйте этот пароль в `SMTP_PASS`
   
   - **Важно:**
     - Используйте именно "Пароль приложения", а не обычный пароль от Gmail
     - Для локальной разработки это безопасно
     - Для продакшена рекомендуется использовать отдельный email или сервис (SendGrid, Mailgun)
   
   - **Другие провайдеры:**
     - Outlook: `smtp-mail.outlook.com:587`
     - SendGrid: `smtp.sendgrid.net:587` (требуется API ключ)
     - Mailgun: `smtp.mailgun.org:587`

#### **Web3 интеграция:**

4. **NEXT_PUBLIC_ALCHEMY_ID**:
   - Зарегистрируйтесь на [Alchemy](https://www.alchemy.com/)
   - Создайте новое приложение (App)
   - Выберите сеть (Ethereum Mainnet, Sepolia и т.д.)
   - Скопируйте API Key из Dashboard
   - Формат: `https://eth-mainnet.g.alchemy.com/v2/YOUR_API_KEY`

5. **NEXT_PUBLIC_WALLETCONNECT_ID**:
   - Зарегистрируйтесь на [WalletConnect Cloud](https://cloud.walletconnect.com/)
   - Создайте новый проект
   - Скопируйте Project ID из настроек проекта

#### **JWT секрет:**

6. **JWT_SECRET**:
   - Сгенерируйте случайную строку (минимум 32 символа)
   - Можно использовать команду: `openssl rand -base64 32`
   - Или онлайн генератор: [generate-secret.vercel.app](https://generate-secret.vercel.app/32)
   - **ВАЖНО**: Храните в секрете, не коммитьте в Git!

#### **API ключи:**

7. **UNISWAP_API_KEY**:
   - Зарегистрируйтесь на [The Graph](https://thegraph.com/) (Uniswap использует The Graph для данных)
   - Или используйте публичный endpoint: `https://api.thegraph.com/subgraphs/name/uniswap/uniswap-v3`
   - Для продакшена рекомендуется получить API ключ для увеличения лимитов
   - Альтернатива: используйте [Uniswap V3 Subgraph](https://thegraph.com/hosted-service/subgraph/uniswap/uniswap-v3) напрямую

### ⚠️ Важные замечания:

- Файл `.env.local` уже должен быть в `.gitignore` (Next.js добавляет его автоматически)
- Никогда не коммитьте файлы с секретами в Git
- Для продакшена используйте переменные окружения на хостинге (Vercel, Railway, etc.)
- Некоторые переменные можно оставить пустыми для локальной разработки (например, UNISWAP_API_KEY)

4. **Запуск в режиме разработки:**
```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:3000

## 🔑 Важные технические детали

### Работа с Uniswap V3 API:

**ВАЖНО**: Прямое открытие позиций через API без подписи транзакций невозможно из-за фундаментальных принципов безопасности блокчейна.

### Решение через Smart Account:

1. **Депонирование средств**: Пользователи отправляют токены на смарт-контракт платформы
2. **Управление позициями**: Смарт-контракт открывает позиции от имени пользователей
3. **Прозрачность**: Все транзакции видны в блокчейне
4. **Вывод средств**: Пользователи могут в любой момент вывести свои средства

### Смарт-контракты (требуется разработка):

```solidity
// contracts/AnySwapVault.sol
contract AnySwapVault {
    // Хранение депозитов пользователей
    mapping(address => mapping(address => uint256)) public deposits;
    
    // Управление позициями Uniswap V3
    function createPosition(...) external;
    function collectFees(...) external;
    function closePosition(...) external;
    function withdraw(...) external;
}
```

## 📁 Структура проекта

```
anyswap-project/
├── app/
│   ├── api/              # API endpoints
│   ├── layout.tsx        # Root layout
│   └── page.tsx          # Home page
├── components/
│   ├── Header.tsx        # Navigation header
│   ├── modals/           # Modal components
│   ├── pools/            # Pool-related components
│   └── dashboard/        # Dashboard components
├── lib/
│   ├── i18n/            # Internationalization
│   ├── db/              # Database utilities
│   └── web3/            # Web3 utilities
├── contracts/           # Smart contracts
├── public/             # Static assets
└── styles/            # Global styles
```

## 🔄 Процесс работы пользователя

1. **Регистрация/Вход**:
   - Email + пароль ИЛИ подключение кошелька
   
2. **Пополнение баланса**:
   - Депонирование токенов на платформу
   - Подтверждение транзакции в кошельке

3. **Создание позиции**:
   - Выбор пула и параметров
   - Установка ценового диапазона
   - Подтверждение создания

4. **Управление**:
   - Мониторинг доходности
   - Сбор комиссий
   - Корректировка диапазонов

5. **Вывод средств**:
   - Закрытие позиции
   - Вывод токенов на кошелек

## 🛡 Безопасность

- **Аудит смарт-контрактов**: Обязателен перед запуском
- **Multi-sig управление**: Для критических операций
- **Rate limiting**: Защита API от DDoS
- **2FA**: Опциональная двухфакторная аутентификация

## 📈 Дальнейшее развитие

- [ ] Разработка и аудит смарт-контрактов
- [ ] Интеграция с большим количеством DEX
- [ ] Автоматические стратегии ребалансировки
- [ ] Мобильное приложение
- [ ] Интеграция с Layer 2 решениями
- [ ] Социальные функции и копитрейдинг

## 📞 Контакты и поддержка

- Email: support@anyswap.io
- Discord: discord.gg/anyswap
- Twitter: @AnySwapIO

## 📝 Лицензия

MIT License - см. файл LICENSE для деталей
